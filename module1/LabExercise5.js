let coffees = ['Flat White', 'Long Black', 'Cappuccino', 'Macchiato', 'Latte']; 
//try creating an array that has 5 elements.

coffees[1] = 'Mocha';
coffees[4] = 'Piccolo';
 //Replace the value of the element at position 1 and 4.

coffees.unshift('Espresso'); //Add a new element to the beginning of the array


let lastCoffee = coffees.pop(); //Remove the element at the end of the array

console.log(coffees);//Print the array to the console.
